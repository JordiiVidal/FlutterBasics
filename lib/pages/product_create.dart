import 'package:flutter/material.dart';

class ProductCreatPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Center(child: Text('Create'),);
  }
}
